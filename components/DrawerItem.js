import {View, Text} from "react-native";

export default DrawerItem = (props) => {
    return (
        <View>
            <Text>
                {props.title}
            </Text>
        </View>
    )
}